# [题目名称]

> 保存位置：`writeups/<题目短名>/writeup.md`。solver/Exp 放同一子目录，派生物放 `writeups/<题目短名>/artifacts/`，并在 `writeups/README.md` 主索引补一行。不要把文件散落在项目根目录。
> 完稿前删除所有方括号提示和本行说明。只填写能由题目材料、实际命令、运行结果或作者记录支持的内容；缺失事实标为“未记录”，不要补写。

## 1. 题目信息

- 题目名称：[名称]
- 题目类型：[Web / Pwn / Reverse / Crypto / Forensics / Misc / OSINT / AI 等]
- 一句话打法：[用一句话点明整条链，让读者先看清全貌再看细节。例：“泄漏 canary 后覆盖返回地址触发 ret2win”“数组绕过强比较 + escapeshellcmd 下用 nl 读 flag”。未解出时写关键卡点。]
- 最终 Flag：`[已验证 Flag；未解出时写“未获得”]`
- 题目附件/服务：[文件名、哈希、服务入口或“无”]

## 2. 题目分析

[说明最初观察到的文件特征、程序逻辑、请求链、漏洞位置、加密结构或取证线索。引用决定性函数、字段、地址、公式或输出，并解释这些证据说明了什么。]

## 3. 解题思路

[说明突破口如何确定、为什么选择这条路线、方法成立需要哪些条件。只保留真实发生且改变判断的失败路线。]

## 4. 解题过程

### 4.1 [关键步骤名称]

[先写观察和本步目的，再给实际命令、请求、调试动作、反编译片段或推导。]

```bash
[实际执行的命令；不适用时更换代码块语言]
```

关键输出：

```text
[只摘录决定性输出；没有保存时明确写“输出未记录”]
```

[解释输出如何验证当前判断，以及为什么继续下一步。根据真实过程增加 4.2、4.3 等小节。]

## 5. 解题代码

文件：[与本 writeup 同目录，例如 `writeups/<题目短名>/solve.py` 或 `exp.py`]

```python
[完整必要代码；若代码保存在独立文件，可引用实际路径并摘录核心部分]
```

依赖与运行方式：

```bash
[安装已锁定依赖的命令]
[运行命令和参数]
```

[说明输入、输出、核心逻辑和成功检查点。手工步骤不可省略时，在这里交代它与代码的衔接。]

## 6. AI 使用说明

[用一到三句准确说明 AI 在分析、推导、代码、调试或文字整理中的实际参与，以及作者亲自完成或复核了什么、关键结果如何通过命令/脚本/题目环境验证。若实际是人主导、AI 仅在对话中提供思路和解释（非自动化代理独立执行），据实写成“对话式/顾问式 AI 辅助”；不要为迎合评审偏好而谎报协助模式。]
