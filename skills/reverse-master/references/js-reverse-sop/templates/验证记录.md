# 验证记录

用途：记录浏览器基线、本地对照、固定输入、路线证明、允许误差和验证结论。

填写规则：
- 不写“已验证”这类空结论。
- 每次验证必须绑定基线样本。
- `验证记录.md` 可由上游阶段提前启动，但归属阶段固定是 `验证`。

## 填写模板

- `基线样本编号`：
- `对照样本编号`：
- `证据编号`：
- `触发阶段`：按 `定位 / 运行时 / 恢复 / 验证` 填写。
- `归属阶段`：固定填 `验证`。
- `验证对象`：填写字段、请求、响应、状态转移、提炼结果或工程化产物。

## 固定输入

- `浏览器侧输入`：
- `本地侧输入`：
- `时间源`：
- `随机源`：
- `会话状态`：

## 路线证明（按需）

- `适配检查`：
- `执行模式`：
- `注入时机`：
- `状态闭合信号`：
- `状态载体`：
- `二跳验证`：

## 对照点

- `检查点1`：
- `检查点2`：
- `检查点3`：

## 允许误差

- `比较项 / 运算符 / 阈值 / 单位`：

## 结果

- `浏览器侧输出`：
- `本地侧输出`：
- `失败样本`：无失败样本时填 `无`。
- `差异定位`：填写差异出现的位置、阶段、证据编号；无则填 `无`。
- `验证结论`：只填 `通过`、`失败`、`部分通过` 之一；填 `部分通过` 时，`失败样本` 与 `差异定位` 不得为 `无`。

## 失败对象（按需）

- `阶段`：
- `代码`：
- `摘要`：
- `证据`：
- `影响`：
- `下一动作`：
